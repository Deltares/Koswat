{
  "name": "koswat_dev_container",
  "build": { 
    "dockerfile": "Dockerfile" 
  },
  "workspaceMount": "source=${localWorkspaceFolder},target=/usr/src/app,type=bind,consistency=cached",
  "workspaceFolder": "/usr/src/app",
  // `git config --global core.autocrlf true` prevents git from showing differences on ALL files when running
  // in the koswat (linux) container
  // `git config --global --add safe.directory ${containerWorkspaceFolder}` allows git to sync with the devcontainer.
  // "postStartCommand": "pixi install & git config --global core.autocrlf true & git config --global --add safe.directory ${containerWorkspaceFolder}",
  "customizations": {
    "vscode": {
      "extensions": [
          "ms-python.python",
          "ms-python.vscode-pylance",
          "ms-python.debugpy",
          "ms-python.black-formatter",
          "ms-python.isort",
          "ms-python.pylint",
          "ms-vscode.test-adapter-converter",
          "njpwerner.autodocstring"
      ]
    }
  },
  "forwardPorts": [3000]
}