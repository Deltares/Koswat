#!/usr/bin/env bash poetry install
# This does not seem to be working due to the line ending (LF instead of CRLF)
# poetry install
# git config --global --add safe.directory "/usr/src/app"
# git config --global core.autocrlf true 